description = "Second brain workflow: os-audit"
prompt = "Read AGENTS.md at the kit root, then .agents/skills/os-audit/SKILL.md relative to that root. Use the canonical workflow for the current request; current user instructions take precedence over kit conventions."
