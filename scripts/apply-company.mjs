#!/usr/bin/env node
// Retained so an old instruction cannot stamp over client source documents.
console.error("Identity stamping is retired. No files were changed. Save identity in context/ and companies/, and approved writing preferences in rules/. Setup needs no scripts. See scripts/README.md.");
process.exitCode = 1;
