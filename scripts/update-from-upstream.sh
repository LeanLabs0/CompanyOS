#!/usr/bin/env bash
# Compatibility stop. Never merge a template into personalized client files.
printf '%s\n' 'Automatic template updates are retired. No files were fetched or merged. Back up, compare a separate new kit, and preserve personal files. See references/backup.md.' >&2
exit 1
